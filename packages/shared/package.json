{
  "name": "@acme/shared",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "pnpm exec eslint --ext .ts src",
    "typecheck": "pnpm exec tsc --project tsconfig.json"
  },
  "dependencies": {
    "dotenv-flow": "^4.1.0"
  }
}
