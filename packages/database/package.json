{
  "name": "@acme/database",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "pnpm exec eslint --ext .ts src",
    "typecheck": "pnpm exec tsc --project tsconfig.json"
  },
  "dependencies": {
    "@acme/shared": "workspace:*"
  }
}
